# runtime dependencies for python-vcon (full)
# (in addition to python-vcon-light-plugins dependencies):

# whisper dependencies
more-itertools
tqdm
transformers
whisper

# stable-ts dependencies:
numpy < 1.25 ; python_version < "3.10"
torch >= 2.1
stable-ts > 2

# redaction dependencies
# Python 3.12 no longer includes setuptools.
# dataprofiler uses setuptools.  So we need to explicitly install it.
setuptools
pandas
tensorflow
scikit-learn
dataprofiler
# datasketches 5.2.0+ requires nanobind which does not support Python < 3.9
datasketches < 5.0.0 ; python_version < "3.9"

